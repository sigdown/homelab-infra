---
- name: Wait until every node is ready
  hosts: all
  gather_facts: false
  any_errors_fatal: true

  tasks:
    - name: Wait for stable Ansible connection
      ansible.builtin.wait_for_connection:
        delay: 3
        connect_timeout: 10
        sleep: 3
        timeout: 300