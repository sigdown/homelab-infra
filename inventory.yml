all:
  children:
    home:
      hosts:
        home_server:
          ansible_host: 192.168.1.110  # homelab local ip
          ansible_user: deploy
    ingress:
      hosts:
        vps:
          ansible_host: 185.28.85.12 # ingress ip
          ansible_user: deploy